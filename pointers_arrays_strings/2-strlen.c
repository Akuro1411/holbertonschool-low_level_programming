#include "main.h"
/**
 * swap_int - check the code
 * @a: - input of function
 * @b: - input of function
 */

int _strlen(char *s)
{
  int i;
  i = 0;
  while (*s[i])
    {
      i++;
    }
  return (i);
}
