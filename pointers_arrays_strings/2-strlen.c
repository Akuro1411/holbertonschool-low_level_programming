#include "main.h"
/**
 * swap_int - check the code
 * @a: - input of function
 * @b: - input of function
 */

int _strlen(char *s)
{
  int i;
  char t;
  i = 0;
  t = *s
  while (t[i])
    {
      i++;
    }
  return (i);
}
