#include "main.h"
0;276;0c/**
 * reverse_array - check the code
 * @a: - input of function
 * @n: - input of function
 * 
 */
void reverse_array(int *a, int n)
{
  int i;
  for (i = n-1; i >= 0; i--)
    {
      return (a[i]);
    }
}
