#include "main.h"
/**
 * _atoi - check the code
 * @s: - input of function
 * Return: last value (Success)
 */
int _atoi(char *s);
{
int i, len;
len = 0;
while (src[len])
len++;
for (i = 0; i < len + 1; i++)
dest[i] = src[i];
return (dest);
}
