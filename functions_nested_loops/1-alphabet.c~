#include "main.h"
void print_alphabet(void);

int main(void);
{
print_alphabet();
_putchar('\n')
return (0);
}
void print_alphabet(void)
{
char letter;
for (letter = 'a'; letter <= 'z'; letter++)
{
_putchar(letter)
}
}
