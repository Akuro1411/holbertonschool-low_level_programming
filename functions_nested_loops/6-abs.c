#include "main.h"

/**
 * print_sign - prints alphabet
 * @a: - input of function
 * Return: Always 0 (Success)
 */

int _abs(int a)
{
  if (a > 0 || a == 0)
    {
      _putchar(a);
	}
  else if (a < 0)
    {
      t = a * -1;
	_putchar(t);
    }
  return (0);
}
